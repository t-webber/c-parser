// mod tree;
// use crate::errors::compile::Res;
// use crate::lexer::types::{Symbol, Token, TokenValue};
// use tree::Node;

// pub fn split_blocks(tokens: Vec<Token>) -> Vec<Vec<Token>> {
//     let mut res = vec![];
//     for token in tokens {
//         if *token.get_value() == TokenValue::Symbol(Symbol::SemiColon) {}
//     }
//     // tokens.into_iter().split()
//     res
// }

// pub fn parse(tokens: Vec<Token>) -> Res<Node> {}
